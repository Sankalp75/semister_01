#include <stdio.h>

int main() {
    int i;

    printf("The first 10 natural numbers are:\n");

    // Loop from 1 to 10
    for (i = 1; i <= 10; i++) {
        printf("%d ", i);
    }

    printf("\n"); // Move to next line after printing

    return 0;
}
